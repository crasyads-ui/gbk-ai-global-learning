"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
export default function Progress(){const [p,setP]=useState(0);useEffect(()=>{const sync=()=>setP(Math.min(100,Math.max(0,Number(localStorage.getItem("gbk_progress")||0))));sync();window.addEventListener("storage",sync);return()=>window.removeEventListener("storage",sync)},[]);return <main className="page"><div className="top"><Link href="/">← Home</Link><h1>📈 My Progress</h1><p>Small steps become real skills.</p></div><section className="card"><h2>{p}% completed</h2><div className="bar"><i style={{width:`${p}%`}}/></div><p>Complete lessons and practice to build your progress.</p><div className="actions"><Link className="btn primary" href="/paths">Continue Learning →</Link><Link className="btn" href="/tutor">Ask AI Tutor</Link></div></section></main>}
