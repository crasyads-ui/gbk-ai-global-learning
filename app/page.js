"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {greetings,labels} from "./lib/languages";
export default function Home(){const [lang,setLang]=useState("English");useEffect(()=>{const sync=()=>setLang(localStorage.getItem("gbk_language")||"English");sync();window.addEventListener("gbk-language-change",sync);return()=>window.removeEventListener("gbk-language-change",sync)},[]);return <main className="page"><section className="hero"><div className="eyebrow">🤖 GBK AI GLOBAL LEARNING V11</div><h1>Learn simply.<br/><span>Practice every day.</span></h1><p>{greetings[lang]} Learn in your language, listen, speak, practice with AI, get clear corrections, and build real-world skills step by step.</p><div className="actions"><Link className="btn primary" href="/paths">{labels.start[lang]||labels.start.English} →</Link><Link className="btn" href="/tutor">{labels.askTutor[lang]||labels.askTutor.English}</Link></div></section><section className="card"><div className="eyebrow">🌍 GLOBAL + YOUR LANGUAGE</div><h2>One platform. Many languages. Real-world skills.</h2><p>Choose a language and a goal. GBK AI is built to make useful skills easier to understand and easier to practice.</p><div className="languageCloud">{Object.keys(greetings).map(x=><span key={x}>{x}</span>)}</div></section><section className="card"><h2>{labels.learn[lang]||labels.learn.English} → {labels.practice[lang]||labels.practice.English} → {labels.improve[lang]||labels.improve.English}</h2><div className="grid three"><div><b>1. {labels.learn[lang]||labels.learn.English}</b><p>Short, practical lessons.</p></div><div><b>2. {labels.practice[lang]||labels.practice.English}</b><p>{labels.speakOrType[lang]||labels.speakOrType.English}</p></div><div><b>3. {labels.improve[lang]||labels.improve.English}</b><p>{labels.correction[lang]||labels.correction.English}</p></div></div></section></main>}
